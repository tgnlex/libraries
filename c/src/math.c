

int add(int x, int y)
{
  int result = x + y;
  printf(result);
  return result;
}

int subtract(int x, int y) 
{
  int result = x - y;
  printf(result);
  return result;
}
int divide(int x, int y) 
{
  int result = x / y;
  printf(result);
  return result;
}
int multiply(int x, int y) 
{
  int result = x * y;
  printf(result);
  return result;
}
